import { Container } from '@mui/material';
import React from 'react';

const LoginScreen: React.FC = () => {
  return <Container maxWidth="md">LoginScreen</Container>;
};

export default LoginScreen;
