import { Container } from '@mui/material';
import React from 'react';

const NotFoundScreen: React.FC = () => {
  return <Container maxWidth="md">Not found</Container>;
};

export default NotFoundScreen;
